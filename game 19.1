import random

print(' Играть \n(yes/no)')
m = str(input())
print()
a = 0

if m == "yes":
    while True:
        foo = ['Футболка', 'шорты', 'улыбка', 'счастье', 'альпинист', 'игрист', 'шафер', 'глаз', 'губы', 'одеяло', 'мыло', 'туалет', 'император', 'князь', 'качель', 'мяч', 'футбол', 'волейбол', 'орёл', 'рыба']

        text = random.choice(foo)
        print(text)

        words = text.split()
        for i, word in enumerate(map(list, words)):
            random.shuffle(word)
            words[i] = ''.join(word)

        print("Введите слово.\nЕсли хотите выйти напишите gg.\nЕсли хотите посмотреть свой счет напишите go.")
        print()
        print(*words)
        m = str(input())
        print()
        if m == text:
            print("You win!")
            print("+10 очков")
            a+=10
        elif m == "gg":
            exit
        elif m == "go":
            print(a)
            print()
        else:
            print("Game over!")
            print("Ваш счет: ", a)
            exit
            break

elif m == "no" :
    exit
