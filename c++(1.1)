#include <iostream>
#include <cmath>
using namespace std;
int main()
{
	double m;
	cout << "Введите угол Альфа: ";
	cin >> m;
	cout << "y= "<< (sqrt(pow((3*m+2),2)-24*m))/(3*sqrt(m)-(2/sqrt(m)))<< endl;
	cout << "z= "<< sqrt(m)<<endl;
	return 0;
}
